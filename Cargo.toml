# Cleon workspace - only pi-mono-rust is used
#
# The previous codex-* dependencies have been removed.
# All provider execution (Codex, Claude, Gemini) now goes through pi-mono-rust.
# See pi-mono-rust/ for the unified provider backend.

[workspace]
members = []
exclude = ["pi-mono-rust"]
# Python package with PyO3 bindings is built via maturin in python/

# Legacy note: The src/main.rs (Codex CLI wrapper) has been removed.
# Use `pi-mono-rust/pi` for CLI usage or `pi_mono` Python bindings via PiMonoBackend.
